let
    type arrint = array of int
    type persona = { nombre : string, edad : int }
	
    function f(a : int, b : string) =
	    let
	        function g(x : int, y : string, r : arrint, p : persona) = 
				print("Hola\n")
	    in
			/**/
	    end            
in
    print("Hola\n")
end
