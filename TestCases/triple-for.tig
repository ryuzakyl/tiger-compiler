let
	var x := 0
in
	printline("Los números de 3 cifras:");
    for i := 1 to 9 do
    (
        for j := 0 to 9 do
        (    
			for k := 0 to 9 do
            ( 
			   printi(i);
			   printi(j);
			   printi(k);
			   print(" ")
			)
		)
    );
	getline()
end
