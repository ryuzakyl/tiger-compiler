let
	type miInt = int
	function IsTrue() : string =
		let
			var x : miInt := 5
		in
			"hola mundo"
		end
	type intArray = array of int
	var p : intArray := intArray[5] of 0
in
	if p <> nil 
	then
		printline("son distintos")
	else
		printline("son iguales");
	getline()	
end

/*Esto está bien semánticamente y debe imprimir "son distintos"*/
