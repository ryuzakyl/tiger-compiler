let
	var x : int := 4
	var x : int := 4
in
	while x > 0 do
		x := (10; if c < 3 then break; x - 1)
end


/*
	Esto no es semánticamente correcto: 
	1-variable x repetida
	2-el expression sequence no retorna valor
	3-la variable c no existe
	4-problemas en la asignación
*/
