let
	type miInt = int
	function IsTrue() : string =
		let
			var x : miInt := 5
		in
			"hola mundo"
		end
	type Person = {Name : string, Age : miInt}
	var p : Person := nil
in
	if p = nil 
	then
		printline("son iguales")
	else
		printline("no son iguales");
	getline()	
end

/*Esto está bien semánticamente y debe imprimir "son iguales"*/
